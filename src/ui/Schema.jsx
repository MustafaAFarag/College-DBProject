import schema from "../assets/schema.jpeg";

export default function Schema() {
  return (
    <div className="flex items-center justify-center">
      <img src={schema} className="w-[100rem] h-[50rem]" />
    </div>
  );
}
